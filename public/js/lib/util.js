// Generated by CoffeeScript 1.6.3
(function() {
  define(['jquery', 'pixi', 'tween'], function($) {
    var UTILS, copy;
    copy = function(obj) {
      return $.extend({}, {}, obj);
    };
    UTILS = {
      clone: copy,
      copy: copy,
      spriteTween: function(sprite, from, to, time, options, onComplete) {
        var tween;
        tween = new TWEEN.Tween(UTILS.copy(from)).to(UTILS.copy(to), time).onUpdate(function() {
          var key, value, _results;
          _results = [];
          for (key in this) {
            value = this[key];
            if (sprite[key] != null) {
              _results.push(sprite[key] = value);
            } else if (key === 'x') {
              _results.push(sprite.position.x = value);
            } else if (key === 'y') {
              _results.push(sprite.position.y = value);
            } else {
              _results.push(void 0);
            }
          }
          return _results;
        });
        if ((options != null ? options.yoyo : void 0) != null) {
          tween.yoyo(options.yoyo);
        }
        if ((options != null ? options.easing : void 0) != null) {
          tween.easing(options.easing);
        }
        if ((options != null ? options.repeat : void 0) != null) {
          tween.repeat(options.repeat);
        }
        if (onComplete != null) {
          tween.onComplete(onComplete);
        }
        return tween;
      }
    };
    return UTILS;
  });

}).call(this);
