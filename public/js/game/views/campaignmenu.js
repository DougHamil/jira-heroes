// Generated by CoffeeScript 1.6.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['jquery', 'jiraheroes', 'gui', 'engine', 'gfx/styles', 'pixi'], function($, JH, GUI, engine) {
    var CampaignMenu;
    return CampaignMenu = (function(_super) {
      __extends(CampaignMenu, _super);

      function CampaignMenu(manager, stage) {
        var _this = this;
        this.manager = manager;
        CampaignMenu.__super__.constructor.apply(this, arguments);
        this.myStage = stage;
        this.heading = new PIXI.Text('Join a Campaign', GUI.STYLES.HEADING);
        this.nameField = new GUI.TextField({
          x: engine.WIDTH / 2,
          y: engine.HEIGHT / 2
        });
        this.newBtn = new GUI.TextButton('New');
        this.backBtn = new GUI.TextButton('Back');
        this.backBtn.position = {
          x: engine.WIDTH - this.backBtn.width,
          y: 0
        };
        this.backBtn.onClick(function() {
          return _this.manager.activateView('HeroMenu');
        });
        this.newBtn.onClick(function() {
          return _this.createNewCampaign();
        });
        this.addChild(this.heading);
        this.addChild(this.backBtn);
        this.addChild(this.newBtn);
        this.newBtn.position = {
          x: engine.WIDTH / 2 + this.nameField.width() + 5,
          y: engine.HEIGHT / 2
        };
        this.nameField.hide();
      }

      CampaignMenu.prototype.createNewCampaign = function() {
        var campaignName,
          _this = this;
        campaignName = this.nameField.getValue();
        if ((campaignName != null) && campaignName !== '') {
          return JH.CreateCampaign(campaignName, 'test', function(data) {
            console.log("Created new campaign: " + data);
            return JH.JoinCampaign(_this.hero._id, data, function(data) {
              console.log("Joined new campaign: " + data);
              return _this.manager.activateView('Campaign', _this.hero, data);
            });
          });
        }
      };

      CampaignMenu.prototype.joinCampaign = function(campaign) {
        var _this = this;
        return JH.JoinCampaign(this.hero._id, campaign._id, function(data) {
          return _this.manager.activateView('Campaign', _this.hero, data);
        });
      };

      CampaignMenu.prototype.deactivate = function() {
        var btn, _i, _len, _ref, _results;
        this.nameField.hide();
        this.myStage.removeChild(this);
        _ref = this.buttons;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          btn = _ref[_i];
          _results.push(this.removeChild(btn));
        }
        return _results;
      };

      CampaignMenu.prototype.activate = function(hero) {
        var _this = this;
        this.hero = hero;
        this.nameField.show();
        this.myStage.addChild(this);
        return JH.GetOpenCampaigns(function(campaigns) {
          var btn, btnHandler, campaign, x, y, _i, _len, _results;
          x = 0;
          y = 50;
          _this.buttons = [];
          btnHandler = function(campaign) {
            return function() {
              return _this.joinCampaign(campaign);
            };
          };
          _results = [];
          for (_i = 0, _len = campaigns.length; _i < _len; _i++) {
            campaign = campaigns[_i];
            btn = new GUI.CampaignButton(campaign);
            btn.position = {
              x: x,
              y: y
            };
            btn.onClick(btnHandler(campaign));
            y += btn.height;
            _this.buttons.push(btn);
            _results.push(_this.addChild(btn));
          }
          return _results;
        });
      };

      return CampaignMenu;

    })(PIXI.DisplayObjectContainer);
  });

}).call(this);
