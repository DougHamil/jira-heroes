// Generated by CoffeeScript 1.6.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['gfx/styles', 'util', 'engine', 'pixi', 'tween'], function(STYLES, Util, engine) {
    var BUTTON_HEIGHT, BUTTON_WIDTH, EndTurnButton;
    BUTTON_WIDTH = 128;
    BUTTON_HEIGHT = 32;
    return EndTurnButton = (function(_super) {
      __extends(EndTurnButton, _super);

      function EndTurnButton() {
        EndTurnButton.__super__.constructor.apply(this, arguments);
        this.width = BUTTON_WIDTH;
        this.height = BUTTON_HEIGHT;
        this.bg = new PIXI.Graphics();
        this.bg.width = this.width;
        this.bg.height = this.height;
        this.bg.beginFill(STYLES.BUTTON_COLOR);
        this.bg.drawRect(0, 0, this.bg.width, this.bg.height);
        this.bg.endFill();
        this.text = new PIXI.Text('End Turn', STYLES.TEXT);
        this.text.position = {
          x: this.bg.width / 2 - this.text.width / 2,
          y: this.bg.height / 2 - this.text.height / 2
        };
        this.addChild(this.bg);
        this.addChild(this.text);
        this.hitArea = new PIXI.Rectangle(0, 0, this.width, this.height);
        this.interactive = true;
      }

      EndTurnButton.prototype.onClick = function(callback) {
        var _this = this;
        return this.click = function() {
          if (callback != null) {
            return callback(_this);
          }
        };
      };

      return EndTurnButton;

    })(PIXI.DisplayObjectContainer);
  });

}).call(this);
