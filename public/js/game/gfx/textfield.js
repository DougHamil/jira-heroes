// Generated by CoffeeScript 1.6.3
(function() {
  define(['jquery'], function($) {
    var TextField;
    return TextField = (function() {
      function TextField(position, text) {
        this.el = $('<input type="text">');
        if (text != null) {
          this.el.val(text);
        }
        this.el.css('position', 'absolute');
        this.el.offset({
          left: position.x,
          top: position.y
        });
        $('#overlay').append(this.el);
      }

      TextField.prototype.width = function() {
        return this.el.width();
      };

      TextField.prototype.height = function() {
        return this.el.height();
      };

      TextField.prototype.hide = function() {
        return this.el.hide();
      };

      TextField.prototype.show = function() {
        return this.el.show();
      };

      TextField.prototype.setValue = function(text) {
        return this.el.val(text);
      };

      TextField.prototype.getValue = function() {
        return this.el.val();
      };

      return TextField;

    })();
  });

}).call(this);
