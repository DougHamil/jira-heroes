// Generated by CoffeeScript 1.6.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['gfx/styles', 'util', 'engine', 'pixi', 'tween'], function(STYLES, Util, engine) {
    var HeroButton;
    return HeroButton = (function(_super) {
      __extends(HeroButton, _super);

      function HeroButton(hero) {
        var texture,
          _this = this;
        HeroButton.__super__.constructor.apply(this, arguments);
        if (hero.media.character.icon != null) {
          texture = PIXI.Texture.fromImage(hero.media.character.icon);
          this.icon = new PIXI.Sprite(texture);
          this.icon.anchor = {
            x: 0.5,
            y: 0.5
          };
          this.icon.position = {
            x: 100,
            y: 150
          };
        }
        this.name = new PIXI.Text(hero.name, STYLES.TEXT);
        this["class"] = new PIXI.Text(hero["class"], STYLES.TEXT);
        this.bg = new PIXI.Graphics();
        this.bg.beginFill(STYLES.BUTTON_COLOR);
        this.bg.drawRect(0, 0, 200, 300);
        this.name.anchor = {
          x: 0.5,
          y: 1.0
        };
        this["class"].anchor = {
          x: 0.5,
          y: 1.0
        };
        this["class"].position = {
          x: 100,
          y: 300
        };
        this.name.position = {
          x: 100,
          y: 300 - (this["class"].height + 20)
        };
        this.from = {
          x: 0,
          y: 0
        };
        this.to = {
          x: 0,
          y: 10
        };
        this.cont = new PIXI.DisplayObjectContainer();
        this.tweens = {
          selected: Util.spriteTween(this.cont, this.from, this.to, 500).repeat(Infinity).yoyo(true).easing(TWEEN.Easing.Sinusoidal.InOut)
        };
        this.cont.addChild(this.bg);
        if (this.icon) {
          this.cont.addChild(this.icon);
        }
        this.cont.addChild(this.name);
        this.cont.addChild(this["class"]);
        this.addChild(this.cont);
        this.cont.interactive = true;
        this.cont.hitArea = new PIXI.Rectangle(0, 0, 200, 300);
        this.interactive = true;
        this.mouseover = function() {
          return _this.animate('selected');
        };
        this.mouseout = function() {
          return _this.animate('none');
        };
      }

      HeroButton.prototype.onClick = function(callback) {
        var _this = this;
        return this.click = function() {
          return callback(_this);
        };
      };

      HeroButton.prototype.animate = function(animation) {
        if (this.activeTween != null) {
          this.activeTween.repeat(0);
        }
        if (animation === 'none') {
          return;
        }
        this.activeTween = this.tweens[animation];
        return this.tweens[animation].repeat(Infinity).start();
      };

      return HeroButton;

    })(PIXI.DisplayObjectContainer);
  });

}).call(this);
