// Generated by CoffeeScript 1.6.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['./styles', 'util', 'engine', 'pixi', 'tween'], function(styles, Util, engine) {
    var CampaignButton, HEIGHT, WIDTH;
    WIDTH = 300;
    HEIGHT = 100;
    CampaignButton = (function(_super) {
      __extends(CampaignButton, _super);

      function CampaignButton(campaignModel) {
        var from, time, to,
          _this = this;
        CampaignButton.__super__.constructor.apply(this, arguments);
        console.log(campaignModel);
        this.name = new PIXI.Text(campaignModel.name, styles.TEXT);
        this["class"] = new PIXI.Text(campaignModel["class"].name, styles.TEXT);
        this.bg = new PIXI.Graphics();
        this.bg.beginFill(styles.BUTTON_COLOR);
        this.bg.drawRect(0, 0, WIDTH, HEIGHT);
        this["class"].position = {
          x: 10,
          y: this.name.height + 5
        };
        this.cont = new PIXI.DisplayObjectContainer();
        this.cont.addChild(this.bg);
        this.cont.addChild(this.name);
        this.cont.addChild(this["class"]);
        this.cont.hitArea = new PIXI.Rectangle(0, 0, WIDTH, HEIGHT);
        this.height = HEIGHT;
        this.width = WIDTH;
        this.addChild(this.cont);
        this.interactive = true;
        to = {
          x: 20,
          y: 0
        };
        from = {
          x: 0,
          y: 0
        };
        time = 250;
        this.tweens = {
          out: Util.spriteTween(this.cont, from, to, time).easing(TWEEN.Easing.Cubic.Out),
          "in": Util.spriteTween(this.cont, to, from, time).easing(TWEEN.Easing.Cubic.Out)
        };
        this.mouseover = function() {
          return _this.animate('out');
        };
        this.mouseout = function() {
          return _this.animate('in');
        };
      }

      CampaignButton.prototype.onClick = function(callback) {
        var _this = this;
        return this.click = function() {
          return callback(_this);
        };
      };

      CampaignButton.prototype.animate = function(animation) {
        if (animation === 'none') {
          return;
        }
        this.activeTween = this.tweens[animation];
        return this.tweens[animation].start();
      };

      return CampaignButton;

    })(PIXI.DisplayObjectContainer);
    return CampaignButton;
  });

}).call(this);
